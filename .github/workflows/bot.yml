name: vinted-psa-bot

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: vinted-psa-bot-run
  cancel-in-progress: false

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - run: pip install -r requirements.txt

      - run: python bot.py
        env:
          POKEMONPRICETRACKER_API_KEY: ${{ secrets.POKEMONPRICETRACKER_API_KEY }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          MIN_DISCOUNT_PERCENT: "-2"
          MAX_DISCOUNT_PERCENT: "40"
          MIN_SALES_SAMPLE: "1"
          MAX_LISTINGS_PER_RUN: "50"
          MAX_DESCRIPTION_FETCHES_PER_RUN: "50"

      - name: Zapisz stan i log z powrotem do repo
        run: |
          git config user.name "vinted-psa-bot"
          git config user.email "actions@users.noreply.github.com"
          git add state.json activity_log.csv
          git diff --cached --quiet || git commit -m "Aktualizacja stanu bota [skip ci]"
          # concurrency group powinien serializowac przebiegi, ale gdyby jednak dwa przebiegi
          # nachodzily na siebie (np. rowny czas natywnego schedule i cron-job.org), push moze
          # zostac odrzucony jako nie-fast-forward - pull --rebase i ponow, zamiast wywalac caly przebieg.
          for attempt in 1 2 3; do
            if git push; then
              break
            fi
            echo "Push nieudany (proba $attempt/3), pobieram najnowszy stan i ponawiam..."
            git pull --rebase origin main
          done
